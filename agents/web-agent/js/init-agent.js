// todo: some extra steps be might added in the future

// Update page title dynamically
document.title = "HTTP Channel API"

// Dynamically load scripts
document.write(`<script src="./js/httpchannel.js"></script>`);
//document.write(`<script src="./rtcpeerconnection.httpchannel.js"></script>`);

window.messagingApiUrl = '/messaging-api/origin-service';
